# Render Blueprint — 대시보드에서 "New +" > "Blueprint"로 이 저장소를 선택하면 자동 배포됩니다.
services:
  - type: web
    name: spellpop
    runtime: node
    plan: free
    buildCommand: npm run build
    startCommand: npm start
    healthCheckPath: /healthz
    autoDeploy: true
